# Tasks

## 1. Репозиторий и каркас

- [x] 1.1 Создать репозиторий `kommunalka` в GitHub через API (токен из `GITHUB_TOKEN`), включить GitHub Pages (branch `main`, корень `/`); проверить: API возвращает `html_url` репозитория и статус Pages `built`
- [x] 1.2 Инициализировать локальный git-репозиторий в `C:\Users\Novitskii_Y\kommunalka` (index.html, css/, js/, README.md, .gitignore с токенами/локальными файлами); проверить: `git status` чистый после коммита
- [x] 1.3 Задеплоить заглушку `index.html` («Коммуналка — скоро») коммитом через GitHub API; проверить: `https://<user>.github.io/kommunalka/` открывается и показывает заглушку

## 2. Supabase: проект, схема, доступ

- [x] 2.1 Создать проект Supabase (регистрация владельца, регион, пароль БД); записать URL и anon-key в `js/config.js` (файл в .gitignore, в репозиторий — `js/config.example.js`); проверить: Healthcheck API `/rest/v1/` отвечает 200
- [x] 2.2 Создать таблицы `objects`, `payment_items`, `periods`, `payments`, `meter_readings` (SQL из design.md D2) и RLS-политики «только авторизованный владелец»; проверить: запрос к таблице без токена отклонён (401/403), с токеном — проходит
- [x] 2.3 Создать пользователя-владельца (email/password) в Supabase Auth, отключить публичную регистрацию; проверить: `signInWithPassword` с реквизитами возвращает сессию, регистрация нового пользователя запрещена

## 3. Фронтенд: авторизация и объекты

- [ ] 3.1 Подключить JS-клиент Supabase локально (без CDN), реализовать экран входа; проверить сценарии specs/auth: без входа видна только форма, неверный пароль — ошибка, успешный вход — доступ
- [ ] 3.2 Реализовать загрузку списка объектов и экран «объекты»; проверить: после входа видны 5 объектов из миграции/тестовых данных
- [ ] 3.3 Реализовать добавление/переименование объекта и настройку его статей/зон воды; проверить сценарии specs/object-management: разные наборы статей у разных объектов отображаются в форме ввода

## 4. Фронтенд: ввод платежей и показаний

- [ ] 4.1 Реализовать форму месяца: год, месяц/склеенный период (label), поля статей объекта, поля показаний; проверить сценарии specs/payment-entry: сохранение, частичный ввод (пустые = 0), редактирование существующего периода
- [ ] 4.2 Реализовать автоматический итог за период (сумма внесённых статей, пересчёт при изменении полей); проверить: итог на экране равен сумме введённых сумм с точностью до копеек
- [ ] 4.3 Реализовать экран истории объекта: хронологический список периодов (sort_key), показания рядом с платежами; проверить: склеенные периоды («Август + Сентябрь») показываются одной строкой в правильном порядке

## 5. Отчёты

- [ ] 5.1 Реализовать отчёт «итоги по всем объектам»: месяц года и год, таблица + столбики на canvas; проверить сценарии specs/reports: суммы совпадают с суммой итогов объектов за период
- [ ] 5.2 Реализовать отчёт «динамика по объекту»: помесячные итоги за год на canvas-графике; проверить: точки в хронологическом порядке, значения совпадают с историей

## 6. Миграция данных

- [x] 6.1 Написать скрипт `migrate/migrate_excel.py` (stdlib: zipfile + XML) разбора `Таблица (13).xlsx`: все 5 листов, месяцы и склеенные периоды, статьи, показания; выгрузить в JSON; проверить: JSON содержит 5 объектов и счёт записей совпадает с количеством непустых строк месяцев в Excel
- [x] 6.2 Реализовать заливку JSON в Supabase (upsert по object+year+label) с pg_dump/backup-скриптом перед заливкой; проверить: повторный запуск не создаёт дублей
- [x] 6.3 Сверить итоги: для каждого листа выборочно 3 месяца, итог сайта == итог Excel (с точностью до копеек, пустые статьи = 0); проверить: отчёт сверки без расхождений

## 7. Финальный деплой и приёмка

- [ ] 7.1 Задеплоить финальную версию на GitHub Pages; проверить: открытие сайта по публичному URL, вход с телефона и с ПК
- [ ] 7.2 Проверить полный сценарий приёмки: вход → ввод нового месяца → итог → отчёт → история; проверить: сценарии из specs проходят вручную
