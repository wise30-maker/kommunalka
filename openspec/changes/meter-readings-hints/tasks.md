# Tasks: Подсказки по показаниям счётчиков

## 1. Расчёты
- [x] 1.1 `js/readings.js`: серия показаний, предыдущее значение, средний месячный расход за год, предложение следующего значения (нули — расход 0, убывающие разницы игнорируются); проверить: модуль подключается в браузере и в Node
- [x] 1.2 `tools/readings_test.js`: прогон расчётов по реальным данным `migrate/data.json`; проверить: по «Фурманова 193» и «Нордик» предложения выглядят правдоподобно, для объекта без показаний сообщается «данных мало»
  - проверено: Фурманова 193 — ванная 469 + 8.969 = 478, электро день 34058 + 607.295 = 34665, кухня расход 0 → 81.192; Вахова 4 — показаний нет вовсе

## 2. Форма периода
- [x] 2.1 Надпись «предыдущие показания: <значение> (<период>)» над названием каждого счётчика; данные объекта догружаются всегда; проверить: у объекта с историей надпись показывает последний период, без истории — «предыдущих показаний нет»
- [x] 2.2 Кнопка «Заполнить по среднему за год»: заполняет только пустые поля, введённые не перетирает, показывает отчёт (заполнено / расход 0 / нет данных); проверить: заполнение совпадает с расчётами стенда

## 3. Приёмка
- [ ] 3.1 Проверка на телефоне: надписи и кнопка видны и работают, отчёт понятен (пользователь)
- [ ] 3.2 Описать в README, как считается средний расход и что показания остаются справочными
