# Tasks: Распознавание квитанции по фото (локальный OCR, без LLM)

## 0. Реквизит «Соответствие ГИС ЖКХ» (схема и UI)
- [x] 0.1 Миграция: `ALTER TABLE payment_items ADD COLUMN IF NOT EXISTS gis_match text` через Supabase Management API; проверить: колонка есть, существующие 30 статей и 298 платежей не пострадали
- [x] 0.2 `js/db.js`: чтение/запись `gis_match` в `listItems`/`upsertItem` (явные колонки, без `select=*`); проверить: значение возвращается и сохраняется
- [x] 0.3 UI настроек объекта: поле «Соответствие ГИС ЖКХ» у каждой статьи (несколько получателей через «;»), сохранение вместе с остальными настройками; проверить: после перезагрузки страницы значение на месте
- [ ] 0.4 Заполнение соответствий пользователем по 5 объектам (делает пользователь); проверить: у каждой используемой статьи есть хотя бы один получатель

## 1. Разведка и ассеты OCR
- [x] 1.1 Скачать в `vendor/tesseract/`: tesseract.min.js (66 КБ), worker.min.js (124 КБ), tesseract-core.wasm.js (4,7 МБ); проверить: файлы на диске
- [x] 1.2 Добавить `rus.traineddata` (~2 МБ, tessdata_fast); проверить: файл скачан и читается движком
- [x] 1.3 `tools/deploy.py`: инкрементальная загрузка — не отправлять blob'ы, чей git-hash совпадает с содержимым репозитория; проверить: повторный деплой без изменений быстрый, ассеты не перезаливаются
- [x] 1.4 Тестовая страница `test-ocr.html` (вне навигации): загрузка изображения → предобработка → OCR → построчный вывод; проверить: на скриншоте «Фурманова 193» текст читается
  - проверено на iPhone (Safari): распознаны все 5 документов и суммы — 795,63 / 4865,20 / 961,07 / 567,56 / 1102,58; наблюдаемые искажения OCR: «56756» (потеряна запятая), «ООО "Магнит" 2» (артефакт иконок), «МУП города Хабаровска "Водок..."» (имя обрезано самой квитанцией)
- [x] 1.5 Отладочный стенд: скрипт на базе Windows OCR (`tools/winocr.ps1`) для прогонов без телефона; проверить: прогон скриншота даёт те же строки

## 2. Парсер (`js/receipt-parse.js`)
- [x] 2.1 Нормализация текста: регистр, кавычки (“ ” « » "), множественные пробелы, OCR-подмены («000»→«ООО», «₽»→«Р»/«е», латиница в кириллице), числа с пробелами-разделителями тысяч и запятой; проверить: «МУП города Хабаровска» и «000 “РЕГСТРОЙКОМ“» нормализуются в ожидаемый вид
  - важно: `\b` в JS не работает с кириллицей — валютный признак ищется через запрет следующего символа
- [x] 2.2 Извлечение сумм документов: шаблон `…,\d{2}` + валютный признак; номера документов (N21/N22) суммой не считаются; проверить: извлекаются 795,63 / 4865,20 / 961,07 / 567,56 / 1102,58
  - проверено на обоих реальных выводах OCR (Safari и Windows): все 5 документов, статья определяется по получателю
- [x] 2.2а Сумма без разделителя (OCR потерял запятую: «56756» вместо «567,56») — не угадывать молча: помечать строку как сомнительную для проверки в превью; проверить: на реальном выводе Safari такая строка попадает в превью как требующая подтверждения
  - проверено: строка «56756 Р» → помечена «похоже 567,56», в итог попадает предложенное значение
- [x] 2.3 Сопоставление со статьями: приоритет 1 — `gis_match` (подстрока), приоритет 2 — словарь синонимов услуги; проверить: каждая строка скриншота получает статью или попадает в «не распознано»
  - проверено: с пустым соответствием «РЕГСТРОЙКОМ» уходит в «не распознано», после заполнения «Магнит;РЕГСТРОЙКОМ» — попадает в «Квартплата»
- [x] 2.4 Суммирование нескольких документов одного получателя в одну статью (Магнит N21+N22 → Квартплата 5826,27); если получатель соответствует разным статьям — пометка «выберите статью»; проверить: контрольный итог 8 292,04 ₽ сходится
  - проверено: Магнит N21+N22 = 5826,27, сумма всех документов 8 292,04 = итог квитанции
- [x] 2.5 Сверка с итогом «К оплате N ₽»: расхождение → предупреждение с разницей; проверить: при подмене одной суммы расхождение показывается
  - проверено на выводе Windows OCR: итог 8 292,04, расхождение 0.00 (строка итога исключена из документов)
- [ ] 2.6 Разбор блока показаний счётчиков (вода х/г с учётом кухня/ванная, электро день/ночь); проверить: показания попадают в нужные поля, при отсутствии — поля пустые и суммы применяются

## 3. UI (форма периода)
- [ ] 3.1 Кнопка «📷 С квитанции» рядом с полями периода; ленивая загрузка воркера с прогрессом; проверить: на iPhone распознавание запускается, прогресс виден
- [ ] 3.2 Превью результата: таблица «статья → сумма», правка сумм, выбор статьи для строк «не распознано», предупреждение о расхождении с итогом; проверить: ни одна строка не теряется
- [ ] 3.3 Кнопки «Применить» (заполняет поля формы, без записи в БД) и «Отмена»; проверить: после «Отмена» форма не изменилась, после «Применить» + «Сохранить» данные в Supabase соответствуют распознанным
- [ ] 3.4 Поведение на телефоне: индикатор распознавания не уезжает под клавиатуру, интерфейс не блокируется; проверить: распознавание 1284×2778 занимает приемлемое время и показывает прогресс

## 4. Приёмка
- [ ] 4.1 Прогон на реальных скриншотах 5 объектов (пользователь): суммы совпадают с «К оплате», расхождения объяснимы; проверить: минимум 3 объекта распознаны полностью без ручных правок
- [ ] 4.2 Деплой на GitHub Pages, проверка с телефона; проверить: ассеты OCR отдаются с сайта (HTTP 200), повторный визит не требует повторной загрузки
- [ ] 4.3 Описать в README проекта: как пользоваться распознаванием и как заполнять «Соответствие ГИС ЖКХ»

## 5. Позже (вне этого change'а)
- [ ] 5.1 PDF-квитанции ДЭК из почты (текстовый слой PDF)
- [ ] 5.2 Service worker для оффлайн-работы распознавания
